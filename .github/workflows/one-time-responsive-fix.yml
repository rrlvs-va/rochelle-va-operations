name: One-time responsive repair cleanup

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/one-time-responsive-fix.yml

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Responsive repair complete"
